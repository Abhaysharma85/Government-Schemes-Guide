spring.application.name=backend
server.port=8080

# Database Configuration (Example for MySQL)
spring.datasource.url=jdbc:mysql://localhost:3306/govschemes?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=password
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# JWT Configuration (Secret key should be kept safe in prod)
jwt.secret=ThisIsAVerySecretKeyForJWTTokenGenerationAndValidationPleaseChangeItInProduction
jwt.expiration=86400000
