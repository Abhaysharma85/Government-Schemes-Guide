spring.application.name=backend
server.port=8080

# MySQL Database Configuration
# Update these credentials to match your local MySQL setup
spring.datasource.url=jdbc:mysql://localhost:3306/govschemes?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=rootioer
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.show-sql=true

# JWT Configuration
# IMPORTANT: Change the secret key in production to a strong, random value
jwt.secret=ThisIsAVerySecretKeyForJWTTokenGenerationAndValidationPleaseChangeItInProduction
jwt.expiration=86400000
